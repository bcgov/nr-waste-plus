# Default to the project's target JDK (see backend/pom.xml -> <java.version>21</java.version>).
# Running under a newer JDK (e.g. GraalVM 22) breaks Lombok annotation processing in test-compile.
# Activate automatically in this directory with:  sdk env
java=21.0.12-oracle
