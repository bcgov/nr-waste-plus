/**
 * Stub service worker — currently inactive.
 *
 * Previously this file was built by `vite-plugin-pwa` with `injectManifest`
 * strategy, which replaced `self.__WB_MANIFEST` with the precache list.
 * The PWA plugin has been removed, so this file is now a no-op placeholder.
 *
 * When offline mode is re-enabled, restore the workbox imports and
 * re-add `vite-plugin-pwa` to the Vite config.
 */

// eslint-disable-next-line no-console
console.debug('[service-worker] offline mode disabled — stub service worker loaded');
