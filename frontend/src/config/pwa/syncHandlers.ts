/* eslint-disable @typescript-eslint/no-unused-vars */
/**
 * Placeholder handler for replaying queued offline mutations during background sync.
 *
 * @returns A resolved promise until sync behavior is implemented.
 */
export const handleSyncMutation = async () => {};

/**
 * Placeholder handler for refreshing cached data during background sync.
 *
 * @param _identifier Optional cache identifier supplied by the sync event.
 * @returns A resolved promise until refresh behavior is implemented.
 */
export const handleSyncRefresh = async (_identifier: string) => {};
